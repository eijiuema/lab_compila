@annot("check", "readInt")
@nce

/*
Matheus da Silva Jesus - 620025
matheus.jesus@dcomp.sor.ufscar.br
*/

/*
  @filename   ok-ger05.ci
      
  @comment    Testa se codigo para readInt é gerado
  corretamente.  
  */

class A
    func m {
        var Int a, b, c, d, e, f;
      
        a = 1;//In.readInt;
        b = 1;//In.readInt;
        c = 1;//In.readInt;
        d = 1;//In.readInt;
        e = 1;//In.readInt;
        f = 1;//In.readInt;

        Out.print: a;
        Out.print: b;
        Out.print: c;
        Out.print: d;
        Out.print: e;
        Out.print: f;
    }
end


class Program
    func run {
        var A a;
      
        Out.println: "";
        Out.println: "Ok-ger05";
        Out.println: "The output should be what you give as input.";
        Out.println: "Type in six numbers";
        a = A.new;
        a.m;
    }   
end
