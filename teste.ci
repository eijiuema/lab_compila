class Store

    var Int n

    func get -> Int {
        return self.n;
    }

    func set: Int n {
        self.n = n;
    }

    func set: Int n, Int m {
        self.n = n;
        Out.print: m;
    }

end

class Program

    func run {
        var Store s;
        var Int a;
        s = Store.new;
        a = In.readInt;
        s.set: a;
        s.set: a, a;
        //s = s.get;
        Out.println: s.get;
    }

end
